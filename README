#My Custom Emacs

copy this file to ~/.emacs and

###########################################################

M-x package-install RET auto-complete

M-x package-install RET auto-complete-c-headers

M-x package-install RET yasnippet

M-x package-install RET iedit-mode

M-x package-install RET flymake-google-cpplint

###########################################################

sudo pip search cpplint

sudo pip install cpplint

for gentoo:

sudo pip install --user cpplint

###########################################################

M-x package-inatall RET flymake-cursor

M-x package-install RET google-c-style

M-x packsge-install RET bs

M-x package-install RET nlinum

##########################################################

for search your includedirectories for gcc type command: 

gcc -xc++ -E -v -

##########################################################
